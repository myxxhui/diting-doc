# 实践记录 · 维度零·AI 投资副驾驶 · 启动期 · Week 04 · M1 持仓体检模块

> [!NOTE] **[TRACEBACK] 实践锚点**
> - **L3 step**: [step_03_W04_持仓体检模块.md](../../../03_原子目标与规约/00_维度零_AI投资副驾驶/stages/stage_1_启动期/steps/step_03_W04_持仓体检模块.md)
> - **DNA**: [_System_DNA/00_co_pilot/dna_stage_1_启动期.yaml](../../../03_原子目标与规约/_System_DNA/00_co_pilot/dna_stage_1_启动期.yaml)
> - **本阶段看板**: [README.md](./README.md)

---

## 一、本周计划（来自 L3）

- 引用：[step_03_W04_持仓体检模块.md](../../../03_原子目标与规约/00_维度零_AI投资副驾驶/stages/stage_1_启动期/steps/step_03_W04_持仓体检模块.md)
- 目标：`health_records` 表、Redis Stream 消费组、`health_change` 落库、`get_dashboard` / `get_detail`、4 色页面与 HTMX、`pytest`（本步 **13** 条：test_health + test_health_consumer；全量 `tests/copilot/` **48 passed**，与同仓 W01～W06 链一致）

---

## 二、实际进展

| §3.x / 项 | 状态 | 说明 |
|---|---|---|
| 3.1 `HealthRecord` 模型 | ✅ | `apps/copilot/db/models.py` 追加 |
| 3.2 `EventConsumer` | ✅ | `apps/copilot/events/consumer.py`；`BUSYGROUP` 使用 `redis.exceptions.ResponseError` |
| 3.3 `health_change` 处理器 | ✅ | `event_logs` + `health_records`；`event_id` 幂等 |
| 3.4 `HealthCheckService` | ✅ | `push_level_to_color`、`get_dashboard`、`get_detail` |
| 3.5 路由与模板 | ✅ | `/api/health/dashboard`、`/api/health/{symbol}`、`/health-dashboard`、`/health-detail/{symbol}`；`templates/health/*` |
| 3.6 `main.py` | ✅ | `include_router(health_routes)`；导航增加「持仓体检」 |
| 3.7 mock 注入脚本 | ✅ | `scripts/inject_mock_health_change.py` |
| 3.8 pytest | ✅ | `test_health.py`（含 step_01 回归 `test_health_returns_ok`）+ `test_health_consumer.py` |
| 3.9 commit / push | ⚠️ | **未执行**（与用户显式提交规则一致） |

### 关键代码变更（工作目录：`diting-src`）

- `apps/copilot/db/models.py`、`events/`、`modules/health_check/`、`main.py`、`templates/health/`、`templates/base.html`
- `scripts/inject_mock_health_change.py`
- `tests/copilot/test_health.py`、`test_health_consumer.py`、`conftest.py`（`asyncio.run(engine.dispose())` 修复 Py3.9 + 异步引擎 teardown）

### mock / Redis 联调

- **Stream / `/health` 数字证据**：只维护一份，见 [W01 · 三-A #l4-w01-redis-verify](./实践记录_W01_后端依赖与服务骨架.md#l4-w01-redis-verify)（〇-1、`XADD`、`XLEN`、`48 passed` 等）。
- **M1 专属**：消费组 `copilot_group`、流 `events:monitor:health_change`；可用 `docker exec diting-redis redis-cli`；**已**用单测覆盖 handler / dashboard / API / HTML。
- **全链路三终端**（consumer + inject + HTTP）：见「三-2」；更严收口见 **step_09**。

---

## 三、测试运行（须含实测证据 — §3.5.3 / §8.4g）

### 命令

```bash
cd /Users/huishaoqi/Desktop/workspace/diting-src
python3 -m pip install -e .
python3 -m pytest tests/copilot/test_health.py tests/copilot/test_health_consumer.py -v --tb=short
python3 -m pytest tests/copilot/ -v --tb=short
```

### 输出（§2 指定子集：13 passed）

```
tests/copilot/test_health.py::test_color_mapping[0-green] PASSED         [  7%]
tests/copilot/test_health.py::test_color_mapping[1-yellow] PASSED        [ 15%]
tests/copilot/test_health.py::test_color_mapping[2-orange] PASSED        [ 23%]
tests/copilot/test_health.py::test_color_mapping[3-red] PASSED           [ 30%]
tests/copilot/test_health.py::test_color_mapping[5-red] PASSED           [ 38%]
tests/copilot/test_health.py::test_color_mapping[-1-green] PASSED        [ 46%]
tests/copilot/test_health.py::test_dashboard_groups_by_color PASSED      [ 53%]
tests/copilot/test_health.py::test_detail_returns_history PASSED         [ 61%]
tests/copilot/test_health.py::test_api_dashboard_endpoint PASSED         [ 69%]
tests/copilot/test_health.py::test_dashboard_html_renders PASSED         [ 76%]
tests/copilot/test_health.py::test_health_returns_ok PASSED              [ 84%]
tests/copilot/test_health_consumer.py::test_handler_writes_two_tables PASSED [ 92%]
tests/copilot/test_health_consumer.py::test_handler_idempotent_on_same_event_id PASSED [100%]

============================== 13 passed in 1.03s ==============================
```

### 全量回归（`tests/copilot/`：48 passed）

```
................................................                         [100%]
48 passed in 1.81s
```

### JSON 烟测（`TestClient`，空库无持仓时结构示意）

- `GET /api/health/dashboard` → **200**，体含 `cards`（四色键）、`summary`（`total` 与四色计数）。

### 执行元信息

- 时间：**2026-05-17**（验证会话）
- 环境：macOS，Python **3.9.6**；`diting-src` `git` 短哈希（验证时工作区）：**455163f**
- 页面截图：**未附**（可选；单测已覆盖 HTML 含「持仓体检」）

### 结果

- **13/13**（step 指定文件）通过；**48/48**（全 `tests/copilot/`，2026-05-17 复跑）通过。
- 失败：无。

---

## 三-A、Redis（〇-1）与 M1 Stream

> **规约**：[steps/README · 〇-1](../../../03_原子目标与规约/00_维度零_AI投资副驾驶/stages/stage_1_启动期/steps/README.md#redis-docker-lifecycle)。**消费组**：`copilot_group`；**流**：`events:monitor:health_change`。重复数字证据见 [W01 #l4-w01-redis-verify](./实践记录_W01_后端依赖与服务骨架.md#l4-w01-redis-verify)。

### 当前结论

| 项 | 结果 |
|---|---|
| pytest | **13**（本子集）+ **48**（全量 `tests/copilot/`） |
| Redis 终端证据 | **见 W01**（本文件不堆叠多轮全文，符合 §8.4g） |

### 用户本机（需留下 Stream 证据时）

1. 按 〇-1：`docker run -d --name diting-redis -p 6379:6379 redis:7-alpine`，`redis-cli ping` → `PONG`。
2. 终端 A：`COPILOT_REDIS_URL=redis://127.0.0.1:6379/0 python3 -m uvicorn apps.copilot.main:app --port 8080`
3. 终端 B：`COPILOT_REDIS_URL=redis://127.0.0.1:6379/0 python3 -m apps.copilot.events.consumer`
4. 终端 C：`python3 scripts/inject_mock_health_change.py 600519 -23 3`
5. **期望**：consumer 无 `Connection refused`；`GET /api/health/dashboard` JSON 反映新体检数据；可选 `docker exec diting-redis redis-cli XINFO STREAM events:monitor:health_change` 观察 `length`。
6. **收尾**：`docker stop diting-redis && docker rm diting-redis`（遵守 〇-1）。

---

## 三-2、复验命令（必填）

```bash
cd /path/to/diting-src
python3 -m pip install -e .

# 准出子集（须 13 passed）
python3 -m pytest tests/copilot/test_health.py tests/copilot/test_health_consumer.py -v --tb=short

# 全量 copilot（须 48 passed）
python3 -m pytest tests/copilot/ -q

# 元数据校验（须含 health_records）
python3 -c "from apps.copilot.db import models; print([t.name for t in models.Base.metadata.sorted_tables])"

# HTTP（启动 uvicorn 后；须 200 且 JSON 含 cards/summary）
python3 -m uvicorn apps.copilot.main:app --port 8080
# 另终端：
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8080/api/health/dashboard
curl -s http://127.0.0.1:8080/api/health/dashboard | python3 -m json.tool | head -n 30
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8080/health-dashboard

# Redis Stream 联调（须先按 〇-1 起 redis:7-alpine；export COPILOT_REDIS_URL=redis://127.0.0.1:6379/0）
python3 -m apps.copilot.events.consumer
# 另终端：
python3 scripts/inject_mock_health_change.py 600519 -23 3
curl -s http://127.0.0.1:8080/api/health/dashboard | python3 -m json.tool
```

---

## 四、偏离与决策

| 偏离 | 原因 | 决策 | 决策人 |
|---|---|---|---|
| **holding_snapshots** 未建表 | L3 checklist 提及可选第二张表；§3.1 正文仅要求 `HealthRecord` | 本步只落库 `health_records` | 执行会话 |
| 本机无 `redis-cli` | — | 使用 **`docker exec diting-redis redis-cli`** | 执行会话 |
| `conftest` 改用 `await engine.dispose()` | Py3.9 下 `sync_engine.dispose()` 在异步引擎池上报 `no current event loop` | `asyncio.run(engine.dispose())` | 执行会话 |

---

## 五、问题与风险

**收口说明**：统一「宽松 → 收紧」的门禁见 L3 [step_09 §1.1](../../../03_原子目标与规约/00_维度零_AI投资副驾驶/stages/stage_1_启动期/steps/step_09_W11_全链路联调.md#l3-step09-tightening) 与 [steps/README · 二-1](../../../03_原子目标与规约/00_维度零_AI投资副驾驶/stages/stage_1_启动期/steps/README.md#l3-steps-loose-to-tight) 对照表。

| 问题 | 影响 | 应对 | 负责人 |
|---|---|---|---|
| M1：Redis **XADD + XLEN + `/health`** 已证；**consumer 常驻 + dashboard** 三终端一体日志未强行跑 | 证据粒度为「流写入 + 健康检查可读」 | 按需「三-2」补跑；**W11** step_09 §1.1 收紧 | 待定 |

---

## 六、下一步

- [ ] [step_04_W05_推荐池模块](../../../03_原子目标与规约/00_维度零_AI投资副驾驶/stages/stage_1_启动期/steps/step_04_W05_推荐池模块.md)
- [ ] （可选）`git commit`：`feat(copilot): W4 M1 持仓体检 4 色卡片 + Redis Stream 订阅 [Ref: .../step_03]`

---

## 七、部署快照（本 step）

**本周无上架**；仅 `diting-src` 本地逻辑与 SQLite schema 扩展。

| 项 | 本 step |
|---|---|
| Chart / diting-infra | **不改** |
| ACR tag | **—** |
| helm release | **—** |

---

## 八、引用

- [02_技术方案与代码架构.md](../../../03_原子目标与规约/00_维度零_AI投资副驾驶/stages/stage_1_启动期/02_技术方案与代码架构.md)

---

## 九、一致性检查（L4 回写）

- [x] 「三」含实测 pytest 输出；「三-2」含完整复验命令
- [x] mock/Redis 与 [W01 #l4-w01-redis-verify](./实践记录_W01_后端依赖与服务骨架.md#l4-w01-redis-verify) 对齐；§8.4g 不堆叠多轮全文
- [x] stage README、steps/README 已更新
- [x] 部署快照占位
