# L3 · 超级个体进化 · 后端服务子模块设计

> [!NOTE] **[TRACEBACK]**
> - **同模块**：[01_目标与边界_设计](./01_目标与边界_设计.md)、[03_接口契约_设计](./03_接口契约_设计.md)
> - **DNA**：`_System_DNA/super_evo/`

## 总览

| # | 子模块 | 英文键 | 部署形态 |
|---|--------|--------|---------|
| 1 | 评测中心 | `eval_center` | 独立服务 / 多实例 + 离线 worker |
| 2 | 反馈采集 | `feedback_collector` | 独立服务 / 多实例 |
| 3 | 知识库 + 长期记忆 | `knowledge_base` | 独立服务 / 多实例 + 向量索引 |
| 4 | 复盘服务 | `retrospective_service` | 独立服务 / 单实例（HA）+ worker |
| 5 | 版本管理（模型 / prompt / 规则） | `version_manager` | 独立服务 / 多实例 |
| 6 | 外部动作边界 | `external_action_boundary` | 独立服务 / 多实例（关键路径） |
| 7 | 用户成长档案 | `user_profile_service` | 独立服务 / 多实例 |

---

## 1. 评测中心（eval_center）

### 主责
- 管理评测集（离线 dataset 与在线 traffic 分流）
- 自动评测任务调度与执行
- 评测报告生成 + 趋势监控

### 内部组件
```
┌────────────────────────────────────────────────┐
│ DatasetManager (CRUD + 版本)                    │
│ TaskScheduler (cron / 触发)                     │
│ Runner                                          │
│   ├─ Offline (跑评测集 → 议会 / 状态机 / 推理) │
│   └─ Online (A/B 流量分流；可逆)                │
│ MetricCollector (按指标汇总)                    │
│ ReportBuilder (HTML / PDF / Markdown)          │
│ TrendMonitor (与历史对比；告警)                 │
└────────────────────────────────────────────────┘
```

### 关键设计
- **评测集是数据资产**：版本化（[07_数据版本控制](../_共享规约/07_数据版本控制规约.md)），变更走审计
- **离线 + 在线分层**：离线先验证；在线灰度小流量后扩大
- **指标可扩展**：核心指标内置；自定义指标走插件

### 失败模式
| 失败 | 保护 |
|------|------|
| 评测集加载失败 | 锁定版本，禁用任务，触发风险事件 |
| 在线 A/B 流量异常 | 自动收敛流量到对照组 |

---

## 2. 反馈采集（feedback_collector）

### 主责
- 接收前端 / 用户的反馈：显式（点赞 / 踩 / 评分 / 备注）+ 隐式（点击 / 停留 / 忽略）
- 接收外部世界回馈（如新闻证伪、状态机长期表现）
- 落库 + 派发到评测中心 / 知识库

### 接入点
- 前端 SDK：埋点 + 显式反馈按钮
- 状态机长期数据：定期跑批从 `state_watch` 拉迁移日志 / Advisory 执行结果
- 数据层 webhook：新事件验证旧结论

### 失败模式
| 失败 | 保护 |
|------|------|
| 入口高并发 | 队列 + 限流 + 异步持久化 |
| 反馈池数据偏倚 | 评测中心做加权 / 去偏 |

---

## 3. 知识库 + 长期记忆（knowledge_base）

### 主责
- 维护结构化知识（实体 / 关系 / 事件 / 规则）+ 嵌入索引
- 提供 RAG 检索接口
- 知识衰减（时效性 / 置信度）

### 数据结构
```
KnowledgeEntry {
  id, type (fact/rule/playbook/glossary),
  subject_ids[], content, embedding_ref,
  source_refs[], confidence, valid_from, valid_to,
  decay_policy, version, created_at, updated_at
}
```

### 内部组件
```
┌──────────────────────────────────────────────┐
│ Ingester (来自评测 / 复盘 / 反馈 / 议会)    │
│ Deduplicator (相似度去重)                    │
│ Embedder (嵌入向量；接 Inference Gateway)    │
│ DecayWorker (定期降权 / 归档)                │
│ Retriever (RAG: hybrid 检索 + rerank)        │
│ FeedbackHandler (用户对知识条目的反馈)       │
└──────────────────────────────────────────────┘
```

### 关键设计
- **去重在写入时**：相似度 > 阈值 → 合并而非新增
- **衰减可配置**：政策类高衰减；术语类低衰减
- **检索默认带可解释性**：返回每条结果的 source_refs + score breakdown

### 失败模式
| 失败 | 保护 |
|------|------|
| 嵌入失败 | 写规则索引兜底；议会接受降级 |
| 检索超时 | 议会拿到部分结果 + 标记 partial |

---

## 4. 复盘服务（retrospective_service）

### 主责
- 周期复盘（每日 / 每周 / 每月）+ 触发式复盘（重大事件 / 用户请求）
- 自动归因：哪些事件 / 工具 / 模型版本影响结果
- 输出复盘报告并归档

### 内部组件
```
┌──────────────────────────────────────────────┐
│ Scheduler (cron + 事件订阅 + 用户请求)        │
│ DataLoader (拉评测 / 反馈 / 状态机 / 议会)    │
│ AttributionAnalyzer (归因模型)                │
│ Storyteller (LLM 写报告)                      │
│ Archiver (s3 + 知识库回写)                    │
└──────────────────────────────────────────────┘
```

### 关键设计
- **归因不只看相关，要看反事实**：尽可能跑 counterfactual（如"若使用旧 prompt 会怎样"）
- **写报告必带证据 + 数据出处**

### 失败模式
| 失败 | 保护 |
|------|------|
| 数据源缺失 | 跳过该维度并标注 |
| LLM 写报告失败 | 退化到模板报告 + 标注 degraded |

---

## 5. 版本管理（version_manager）

### 主责
- 注册 / 灰度 / 上线 / 回滚 模型 / prompt / 规则 / 评测集
- 提供版本元数据查询（哪个版本来自哪条评测、谁批准的）

### 数据结构
```
VersionEntry {
  artifact_type (model/prompt/rule/dataset),
  artifact_id, version, status (draft/staging/canary/prod/rolled_back),
  approver, approved_at, eval_report_ref,
  rollout_strategy { canary_pct, ramp_steps },
  signature, created_at
}
```

### 关键设计
- **强制评测前置**：从 staging → canary 必须有评测报告
- **回滚一键**：任意版本可回到上一个 prod
- **签名机制**：上线版本带签名，防篡改

### 失败模式
| 失败 | 保护 |
|------|------|
| 灰度异常 | 自动收敛流量；触发风险事件 |
| 回滚失败 | 多副本快照；强制人审 |

---

## 6. 外部动作边界（external_action_boundary）

### 主责
- 全系统对外动作的**唯一出口**
- 接收任意模块的 `ExternalActionRequest` → 调极寒防御门禁 → 通过后调 [External API Port](../_共享规约/05_接口抽象层规约.md)
- 强制 idempotency_key + 签名 + 审计

### 内部组件
```
┌──────────────────────────────────────────────┐
│ Receiver (接 ExternalActionRequest)           │
│ IdempotencyGuard (去重 + 缓存首次结果)        │
│ GateClient → 极寒防御 decision_gate           │
│   (失败 / reject → 拒绝业务 + 通知)           │
│ ApiPortClient (调 External API Port)         │
│ AuditEmitter (写极寒防御 audit)               │
│ ResultDispatcher (返调用方)                   │
└──────────────────────────────────────────────┘
```

### 失败模式
| 失败 | 保护 |
|------|------|
| 门禁失败 | 业务拒绝 + 通知调用方 |
| 第三方 API 失败 | 重试（带 idempotency）；超阈值熔断 |
| idempotency 冲突 | 返回首次结果，不重复调用第三方 |

---

## 7. 用户成长档案（user_profile_service）

### 主责
- 维护单用户的偏好 / 历史 / 习得能力
- 接受用户对档案的查看 / 编辑 / 导出（GDPR 友好）

### 数据
- `preferences`（通知风格、节奏、风险偏好）
- `history`（交互历史；隐私脱敏后）
- `habits`（系统观察到的习惯，用户可见可改）
- `growth_metrics`（习得指标，按时间趋势）

### 关键设计
- **用户拥有数据**：用户可导出 / 删除
- **不跨用户共享**（除非用户显式同意）

## 共享依赖

| 依赖 | 用途 |
|------|------|
| [05_接口抽象层 §Inference Gateway / External API Port](../_共享规约/05_接口抽象层规约.md) | 嵌入 / LLM / 第三方 API |
| [06_动态配置中心](../_共享规约/06_动态配置中心规约.md) | 评测开关 / 灰度比例 / 衰减参数 |
| [07_数据版本控制](../_共享规约/07_数据版本控制规约.md) | 评测集 / 知识库 / 模型版本 |
| [10_运营治理与灾备](../_共享规约/10_运营治理与灾备规约.md) | 外部动作合规 / DR |
| [极寒防御](../极寒防御/README.md) | 外部动作门禁 + 审计 |
| [纵深进攻](../纵深进攻/README.md) | 议会 / 卡片 / Session 数据来源 |
| [状态机监控](../状态机监控/README.md) | 迁移 / Advisory 反馈来源 |
