# 规约文档（AI 可读）：03_原子目标与规约/_共享规约/04_全链路通信协议矩阵.md
# 说明：索引 03_原子目标与规约/_System_DNA/global_const.yaml 中 protocols 配置。

protocols:
  expert_protocol:
    $ref: "global_const.yaml#protocols.expert_protocol"
  verdict_protocol:
    $ref: "global_const.yaml#protocols.verdict_protocol"
  execution_protocol:
    $ref: "global_const.yaml#protocols.execution_protocol"
  risk_protocol:
    $ref: "global_const.yaml#protocols.risk_protocol"
