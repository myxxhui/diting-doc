# 601138 售卖条件监控 · 要做什么

> **一句话**：601138 五种卖出条件 **SP1~SP5 上游 + sell_signal + 邮件** 全链已通（tier-1 + tier-2 platform Redis）。  
> 节拍对齐：[14_表 §4](./14_六维度启动期统一节奏表.md) · 持仓 SoT：`diting-src/data/config/my_holdings.yaml`

---

## 1. 必做项（功能闭环 · 按 ①→② 顺序）

> **① 没做完，② 没东西可发。** ③④ 不挡邮件闭环。

| 状态 | 项 | 做什么 | 在哪改 | 怎么验 | Cursor 推荐模型 |
|:---:|---|---|---|---|---|
| ✅ | **必做 ①** | 条件触发后写入 Redis `events:exit:sell_signal`（冲突仲裁 **SP1 > SP3 > SP5 > SP2 > SP4**） | D4 **step_07** · `diting-src` | `make exit-step07-all` | Claude Sonnet 4.6 |
| ✅ | **必做 ②** | 订阅上面 stream，发 **🔴 邮件**（不静默） | D0 **step_05** · `diting-src` | `make copilot-step05-all` · 全链 `make copilot-step05-chain-e2e` | Composer 2.5 Fast |
| ✅ | 建议 ③ | 601138 持续产出 `health_change`（SP3 上游） | D3 **step_07** | `make watch-step07-all` | Sonnet 4.6 |
| ✅ | 建议 ④ | 601138 配 thesis + `timer_signal`（SP5 上游） | D2 **step_05** | `make deep-step05-all` | Sonnet 写代码；运行时 **Opus 4.7**（`AIDispatcher`） |
| ✅ | 可选 | 云上 Web 四色页 | P **step_03** + Helm copilot · `diting-infra` | `curl http://8.217.158.218:30080/health-detail/601138` | Sonnet 4.6 |

**L3 实践**：[D4 step_07](../04_维度四_卖出决策/stages/stage_1_启动期/steps/step_07_冲突处理与回测.md) · [D0 step_05](../00_维度零_AI投资副驾驶/stages/stage_1_启动期/steps/step_05_告警系统.md)

---

## 2. 进度（tier 分开看，别混）

| tier | 状态 | 做什么 | 怎么验 |
|:---:|---|---|---|
| **tier-1 本机** | ✅ | D4 step_07 发 `sell_signal`；D0 step_05 消费发 🔴 邮件 | `make deps && make exit-step07-all` · `make copilot-step05-all` |
| **tier-2 云上 K3s** | ✅ **P0+P1** | platform @ **8.217.158.218** · Copilot 消费 sell_signal → 🔴 邮件 | `make copilot-step05-tier2-e2e` · alert_logs email ok |
| **可选 Web** | ✅ 已部署 | Helm copilot → `:30080/health-detail/601138` | `curl http://8.217.158.218:30080/health` |

> **之前标 ✅ 的含义**：仅 **tier-1**（开发机/ECS 上本机 Docker Redis + pytest + 真 SMTP），**不等于** prod K3s 已跑通。当前 **tier-2 P1 也已验收**（2026-05-29）。

---

## 3. 部署与建议项（均已验收）

| 优先级 | 做什么 | 工作目录 | 验收 |
|:---:|---|---|---|
| **P0** | 拉 prod **kubeconfig** + 起 platform 栈 | `diting-infra` | ✅ `config-diting-prod` · `30379` @ 8.217.158.218 · smoke FAIL=0 |
| **P0** | `EXIT_REDIS_URL` 与 `COPILOT_REDIS_URL` **同一 Redis 同一 db** | `diting-src/.env` | ✅ `redis://8.217.158.218:30379/0` · K3s secret 同步 |
| **P1** | exit-engine + copilot 上 K3s · M4 邮件链 | infra + `diting-src` | ✅ XADD → Copilot consumer · `alert_logs` email ok @ 2026-05-29 |
| ✅ | D3 step_07 health_change 真流 | `diting-src` | `make watch-step07-all` · platform Redis XLEN≥1 @ 2026-05-29 |
| ✅ | D2 step_05 601138 timer | `diting-src` | `make deep-step05-all` · Opus timer + Redis xadd @ 2026-05-29 |

---

## 4. 数据链

```
行情 + 持仓
  → D2 step_05 → timer_signal      ✅ 601138 Opus · platform Redis
  → D3 step_07 → health_change     ✅ tier-1 · tier-2（platform Redis）
  → D4 step_03~06 算 SP1~SP5       ✅
  → D4 step_07 → sell_signal       ✅ tier-1 · tier-2
  → D0 step_05 → 邮件              ✅ tier-1 · tier-2（2026-05-29）
```

---

## 5. 五种条件（已实现，不用重写）

| 条件 | 规则（启动期） | 状态 |
|---|---|---|
| SP1 止损 | 浮亏 ≤ -15% | D4 step_03 ✅ |
| SP2 止盈 | 浮盈 ≥ +30%（3 日缓冲） | D4 step_04 ✅ |
| SP3 逻辑失效 | D3 发 `health_change` | D3 step_05 + D4 step_05 ✅ |
| SP4 再平衡 | 仓位偏离阈值 | D4 step_06 ✅ |
| SP5 财报窗口 | D2 发 `timer_signal` | D4 step_05 ✅ · **601138 Opus timer** ✅ |

601138 现状：SP2 约 +13%，**未触发卖出**；必做 ①② 出口已接。

---

## 6. 本机跑不通时

```
ModuleNotFoundError: No module named 'sqlalchemy'
```

**意思**：用了系统自带 `python3`，没装项目依赖。

```bash
cd diting-src
make deps
make copilot-step05-all    # 自动用 .venv/bin/python3
```

---

## 修订记录

| 日期 | 变更 |
|---|---|
| 2026-05-29 | 首版 |
| 2026-05-29 | tier-1：D4 step_07 + D0 step_05 本机验收 |
| 2026-05-29 | **tier-2 P1**：`copilot-step05-tier2-e2e` · alert_logs email ok |
| 2026-05-29 | **D2 step_05**：601138 thesis + Opus timer · `deep-step05-all` 绿 |
